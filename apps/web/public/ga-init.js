// Google Analytics initialization.
// window.dataLayer, gtag(), and Consent Mode defaults are set
// in the synchronous inline boot script in <head> (layout.tsx),
// so they are guaranteed to exist before this file executes.
gtag('js', new Date());
gtag('config', 'G-60RGP979SG');
